{
  "name": "semantius-self-hosted",
  "version": "1.0.0",
  "description": "Self-hosting stack for Semantius — Docker Compose and Dokploy",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  }
}
